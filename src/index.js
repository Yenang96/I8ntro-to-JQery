console.log("Local JS is running!");


$(document).ready(function() {

    // Find the contact form and toggle it
    //Find the "contact us" button
    $(".toggleButton").click(function() {
        //Find the contact form

        $("form").toggle();
    })
    )}